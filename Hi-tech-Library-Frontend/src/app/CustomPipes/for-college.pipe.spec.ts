import { ForCollegePipe } from './for-college.pipe';

describe('ForCollegePipe', () => {
  it('create an instance', () => {
    const pipe = new ForCollegePipe();
    expect(pipe).toBeTruthy();
  });
});
